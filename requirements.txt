# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.135.3
uvicorn[standard]==0.44.0
python-multipart==0.0.24        # required for FastAPI Form() parsing

# ── AI services ───────────────────────────────────────────────────────────────
openai==2.30.0                  # v2 client — max_completion_tokens replaces max_tokens
cohere==5.21.1                  # v5 client with ClientV2 and updated rerank API
chromadb==1.5.6                 # PersistentClient, get_or_create_collection

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.49
psycopg[binary]==3.3.3          # psycopg3 — use postgresql+psycopg dialect in SQLAlchemy

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1       # BaseSettings with .env support

# ── Messaging ─────────────────────────────────────────────────────────────────
twilio==9.10.4

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==9.0.2
pytest-asyncio==1.3.0
httpx==0.28.1                   # required by FastAPI TestClient

# ── Known transitive version ceilings imposed by chromadb → opentelemetry ────
# These cannot be upgraded until chromadb ships an opentelemetry upgrade:
#   importlib-metadata < 8.8.0   (opentelemetry-api 1.40.0 constraint)
#   protobuf < 7.0               (opentelemetry-proto 1.40.0 constraint)
#   pydantic-core is pinned by pydantic 2.12.5
